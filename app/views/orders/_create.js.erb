$("ul.errors").html("");
<% if @order.errors.any? %>
  <% @order.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"));
  <% end %>
<% else %>
  $(".orders-index").html("<%= escape_javascript(render 'index') %>");
  $("#order-modal").modal("hide");
 
<% end %>