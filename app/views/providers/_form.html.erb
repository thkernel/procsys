<%= form_with(model: provider, local: false) do |form| %>
    <div class="modal-body">
      <ul class="errors">

          <% @provider.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                      <% end %>
        </ul>

        <div class="row">
            <div class="col-md-12">
                <div class="form-group">
                  <%= form.label "Nom" %>
                  <%= form.text_field :name, id: :provider_name, class: "form-control", placeholder: "Nom" , required: true %>
                </div>

               

                <div class="form-group">
                  <%= form.label "Adresse" %>
                  <%= form.text_field :address, id: :provider_address, class: "form-control", placeholder: "Adresse" %>
                </div>

                <div class="form-group">
                  <%= form.label "Ville" %>
                  <%= form.text_field :city, id: :provider_city, class: "form-control", placeholder: "Ville" %>
                </div>
                 <div class="form-group">
                  <%= form.label "Pays" %>
                  <%= form.text_field :country, id: :provider_country, class: "form-control", placeholder: "Pays" %>
                </div>

               

                <div class="form-group">
                  <%= form.label "Téléphone" %>
                  <%= form.text_field :phone, id: :provider_phone, class: "form-control", placeholder: "Téléphone" %>
                </div>
            </div><!-- end col -->
        </div><!-- end row -->
        <div class="modal-footer">
              <%= link_to "Annuler", "#", class: "btn", data: {dismiss: "modal"} %>
              <%= form.submit "Enregistrer", class: "btn btn-primary",  "data-loading-text"=>"Loading...", autocomplete: "off" %>
        </div>
<% end %>

    
