<%= form_with(model: order_type, local: true) do |form| %>
  <% if order_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_type.errors.count, "error") %> prohibited this order_type from being saved:</h2>

      <ul>
      <% order_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :order_type_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :order_type_description %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status, id: :order_type_status %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :order_type_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
