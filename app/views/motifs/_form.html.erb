<%= form_with(model: motif, local: true) do |form| %>
  <% if motif.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(motif.errors.count, "error") %> prohibited this motif from being saved:</h2>

      <ul>
      <% motif.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :motif_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :motif_description %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status, id: :motif_status %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :motif_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
