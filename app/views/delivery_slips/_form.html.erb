<%= form_with(model: delivery_slip, local: true) do |form| %>
  <% if delivery_slip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(delivery_slip.errors.count, "error") %> prohibited this delivery_slip from being saved:</h2>

      <ul>
      <% delivery_slip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ref %>
    <%= form.text_field :ref, id: :delivery_slip_ref %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id, id: :delivery_slip_order_id %>
  </div>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.text_field :customer_id, id: :delivery_slip_customer_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :delivery_slip_user_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :delivery_slip_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
