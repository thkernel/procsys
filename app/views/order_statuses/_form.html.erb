<%= form_with(model: order_status, local: true) do |form| %>
  <% if order_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_status.errors.count, "error") %> prohibited this order_status from being saved:</h2>

      <ul>
      <% order_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :order_status_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :order_status_description %>
  </div>


  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status, id: :order_status_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
